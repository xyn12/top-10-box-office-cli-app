require "spec_helper"

RSpec.describe Top10BoxOffice do
  it "has a version number" do
    expect(Top10BoxOffice::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
